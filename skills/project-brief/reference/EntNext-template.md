{
  "PDI Project Brief - EnterpriseNext (Menu Code Migration)": {
    "prefix": "briefentnext",
    "body": [
      "# Project Brief: ${1:Menu Code} — ${2:Screen Title}",
      "**Status:** ${3|Draft,In Review,Approved|}",
      "**PM:** Elly Sheppard",
      "**Product / Area:** PDI EnterpriseNext (SaaS) — ${4:Domain e.g. Accounts Payable, Cash Management, Administration, Desktop}",
      "**Legacy Menu Code:** ${1:Menu Code}",
      "**Parent Epic:** Enterprise Retail Engineering — Bootcamp 2.0 (ADO 1365330)",
      "**Reference Migration:** APCFG (ADO 1365334, PR 178922) — use as the shape-of-the-work example",
      "**Date:** ${5:date}",
      "",
      "---",
      "",
      "## Pre-Brief Research Checklist",
      "Complete or mark each item before drafting the brief.",
      "",
      "- [ ] ADO Feature work item created under epic 1365330: ${6:ADO link or TBD}",
      "- [ ] Legacy Enterprise help page reviewed: ${7:https://help.pditechnologies.com/enterprise/#/home/XXXX/X/X or TBD}",
      "- [ ] Legacy WinForms screen walked through (forms, tabs, fields, validations, conditional UI): ${8:summary or TBD}",
      "- [ ] Control hierarchy documented for `research.md` (group boxes, conditional panels, spatial layout — React tree must mirror it): ${9:summary or TBD}",
      "- [ ] Legacy screen screenshots captured to `docs/images/${1:Menu Code}-*.png`: ${10:status or TBD}",
      "- [ ] Target EnterpriseNext API service identified (e.g. AccountsPayable.Api, CashManagement.Api, Administration.Api, Desktop.Api): ${11:service name or TBD}",
      "- [ ] Legacy stored procedures / tables / views identified (READ-ONLY — zero schema changes allowed): ${12:list or TBD}",
      "- [ ] Shared / reusable components identified (e.g. GLAccountSelector, lookups, dropdown sources): ${13:list or TBD}",
      "- [ ] Permissions / role gating reviewed (legacy role keys, EnterpriseNext Okta OIDC / role mapping): ${14:summary or TBD}",
      "- [ ] Audit expectations confirmed (which entity fields need `[Auditable]` with human-readable old/new via `AuditChangeDetector`): ${15:summary or TBD}",
      "- [ ] Concurrency expectations confirmed (`rowVersion` round-trip required for any `ts`-bearing entity): ${16:confirmed or TBD}",
      "- [ ] Engineering canon reviewed: `CLAUDE.md`, `docs/guardrails.md`, `docs/workflow.md`, `.specify/memory/constitution.md`: ${17:confirmed or TBD}",
      "- [ ] `CONTEXT.md` in any adjacent migrated feature reviewed for pattern reuse: ${18:link or TBD}",
      "- [ ] SMEs / reviewers identified (functional owner, original developer if known): ${19:names or TBD}",
      "- [ ] Confluence: Enterprise Retail - Bootcamp 2.0 page reviewed: ${20:link or default https://pdisoftware.atlassian.net/wiki/spaces/EPD/pages/13047332997}",
      "",
      "---",
      "",
      "## 1. Problem Definition",
      "",
      "**What problem are we solving?**  ",
      "${21:Migrate the legacy Enterprise WinForms screen ${1:Menu Code} (${2:Screen Title}) to the EnterpriseNext SaaS stack (React + .NET 8+ Web API) at feature parity, so customers on EnterpriseNext can perform the same back-office workflow without the legacy desktop client.}",
      "",
      "**Who is affected?**  ",
      "${22:Back-office users (e.g., AP clerks, accountants, system administrators) on EnterpriseNext who need access to ${2:Screen Title}; downstream workflows that depend on the data this screen maintains; customers migrating from on-prem Enterprise to EnterpriseNext SaaS.}",
      "",
      "**What is inefficient, risky, confusing, or broken today?**  ",
      "${23:Screen does not yet exist in EnterpriseNext; users on EnterpriseNext cannot complete the ${2:Screen Title} workflow, blocking SaaS migration for accounts that use it.}",
      "",
      "**Why now?**  ",
      "${24:Sequenced delivery against the Bootcamp 2.0 menu-code migration plan; required to reach feature parity for SaaS GA / customer migration commitments.}",
      "",
      "---",
      "",
      "## 2. Current State / Existing Functionality (Legacy Enterprise)",
      "",
      "**What exists today in legacy Enterprise?**  ",
      "${25:Describe the WinForms screen: navigation path (menu code ${1:Menu Code}), purpose, primary actions (view / edit / save / validate), tab structure if any, and notable behaviors. Reference the legacy help page screenshot/URL.}",
      "",
      "**Tabs / sub-sections in the legacy screen:**",
      "- ${26:Tab 1 name — what it controls}",
      "- ${27:Tab 2 name — what it controls}",
      "- ${28:Tab 3 name — what it controls (or N/A)}",
      "",
      "**Where does the current workflow occur?**  ",
      "${29:Back office, finance/accounting, or corporate admin — not store-facing. Specify if it is also triggered from batch jobs, integrations, or reports.}",
      "",
      "**Known legacy limitations / pain points:**",
      "- ${30:WinForms-only, no browser access}",
      "- ${31:Limitation specific to this screen — e.g., poor validation messaging, no audit trail, awkward UX}",
      "",
      "**Type of change:**  ",
      "Migration / Modernization — parity port of an existing legacy screen to EnterpriseNext SaaS. Not a redesign unless explicitly called out below.",
      "",
      "**Legacy source location (READ-ONLY — never modify):**  ",
      "${32:Path under `Enterprise/` or `EnterpriseWeb/` if known. Legacy code is the source of truth for parity analysis; it is not modifiable and is not tracked in the EnterpriseNext repo.}",
      "",
      "**Intentional behavior changes from legacy (if any):**",
      "- ${33:None expected — full parity / list specific intentional deltas (e.g., dropping a deprecated option, replacing a field, modernizing validation messages)}",
      "",
      "---",
      "",
      "## 3. Key Terminology / Glossary",
      "",
      "| Term | Meaning | Notes / Source |",
      "|---|---|---|",
      "| ${1:Menu Code} | ${34:Full screen name and what it does} | Legacy Enterprise menu code |",
      "| EnterpriseNext | SaaS / browser-based replacement for legacy Enterprise (React + TypeScript + Vite + MUI 6 on the frontend, .NET 8+ Web API + Dapper on the backend) | repo: `EnterpriseNext` |",
      "| Bootcamp 2.0 | Engineering program that migrates legacy Enterprise menu codes to EnterpriseNext one at a time | ADO epic 1365330 |",
      "| Vertical Slice | One menu code delivered end-to-end (Analyze → API → UI → Test → Validate → Complete) before moving on | Constitution principle; see `docs/workflow.md` |",
      "| Parity | Output behavior of the new screen matches legacy for the same inputs and permissions; no new features added during migration | Constitution principle |",
      "| Change-Auditor Agent | Mandatory automated review of any commit referencing a `US-*` story ID; PreToolUse Bash hook blocks unaudited `US-*` commits | See `docs/workflow.md` |",
      "| ${35:Domain term} | ${36:meaning} | ${37:notes or source} |",
      "",
      "---",
      "",
      "## 4. Operational Context",
      "",
      "**Where does this matter operationally?**  ",
      "${38:Back office / corporate / accounting context. Note any integrations (e.g., GL postings, AP invoice export, payment files, RabbitMQ messaging) and whether the screen is referenced by batch jobs, APIs, reports, or other menu codes.}",
      "",
      "**How often does this happen?**  ",
      "${39:Configuration screens are usually low-frequency (set once, edited rarely). Specify if this is high-frequency (e.g., daily reconciliation) instead.}",
      "",
      "**What happens if this fails or is missing in EnterpriseNext?**  ",
      "${40:Customers on EnterpriseNext cannot complete the workflow; SaaS migration blocked for accounts that use this screen; downstream processes that depend on the configuration/data may run with stale or default values.}",
      "",
      "---",
      "",
      "## 5. Users / Personas",
      "",
      "### Persona 1",
      "",
      "**Role:** ${41:e.g., AP Clerk, Accountant, System Administrator, Corporate Finance}  ",
      "**Goal:** ${42:What they want to accomplish on this screen}  ",
      "**Trigger:** ${43:What causes them to open this screen}  ",
      "**Success looks like:** ${44:What \"done\" means for them in a single session}",
      "",
      "### Persona 2 (optional — list only if a second role meaningfully interacts with this screen)",
      "",
      "**Role:** ${45:Role Name or N/A}  ",
      "**Goal:** ${46:answer}  ",
      "**Trigger:** ${47:answer}  ",
      "**Success looks like:** ${48:answer}",
      "",
      "---",
      "",
      "## 6. Core User / System Scenarios",
      "",
      "### Happy Path (parity workflow)",
      "",
      "1. ${49:User opens ${1:Menu Code} from the EnterpriseNext navigation}",
      "2. ${50:Screen loads existing configuration / data from the backend API via React Query (parity with legacy load)}",
      "3. ${51:User edits one or more fields across the relevant tab(s) using React Hook Form}",
      "4. ${52:Validation runs (client + server) matching legacy rules; field-level errors surfaced inline}",
      "5. ${53:User saves; backend persists the change, writes `[Auditable]` change records, and returns updated `rowVersion`; UI confirms success}",
      "",
      "### Additional Scenarios",
      "",
      "- ${54:Read-only view for users without edit permission (server-enforced)}",
      "- ${55:Cancel / navigate away with unsaved changes — `useBlocker` + `useUnsavedChangesWarning(isDirty)` prompts the user}",
      "- ${56:Other scenario specific to this screen or TBD}",
      "",
      "---",
      "",
      "## 7. Acceptance Criteria (Parity-Focused)",
      "",
      "The feature works when:",
      "",
      "- All fields, tabs, dropdowns, and conditional UI from the legacy screen are present in EnterpriseNext (control hierarchy mirrors the legacy spatial layout documented in `research.md`)",
      "- All legacy validation rules produce equivalent results (success and error states) in the new screen",
      "- Saving with identical inputs produces the same persisted data as the legacy screen (zero schema changes; SPs unchanged)",
      "- Role-based access matches legacy: same users can view/edit; unauthorized users blocked server-side",
      "- `[Auditable]` change entries written for every persisted change with human-readable old/new values; field names match on-screen captions",
      "- Optimistic concurrency works end-to-end: `rowVersion` sent on save, returned in response, 409 surfaces `<ConcurrencyConflictDialog>` via `useConcurrencyConflict()`",
      "- Screen renders correctly in en, es-MX, and fr-CA locales; no hardcoded strings (all via `react-i18next`)",
      "- Screen works in both light and dark MUI themes",
      "- Meets WCAG 2.1 AA: 4.5:1 contrast, keyboard navigation, ARIA labels, focus order",
      "- Tests pass: Vitest unit + integration, xUnit backend, ≥1 Playwright E2E covering the primary workflow; ≥80% line coverage on new code",
      "- Vertical-slice discipline holds: no UI without backing API, no API without tests; backend lands first per `docs/workflow.md §1`",
      "- Change-auditor agent run before commit; commit message ends with `[audited]` for any `US-*` story",
      "- ${57:Screen-specific acceptance condition}",
      "- ${58:Screen-specific acceptance condition or N/A}",
      "",
      "---",
      "",
      "## 8. Data & Integration Context",
      "",
      "**Backing data (legacy — READ-ONLY):**  ",
      "${59:Tables, views, and stored procedures the legacy screen reads/writes. Include schema/owner if known. Zero schema changes allowed — no `ALTER`, no new SPs, no SP modifications.}",
      "",
      "**Target API service in EnterpriseNext:**  ",
      "${60:e.g., AccountsPayable.Api / CashManagement.Api / Administration.Api / Desktop.Api}",
      "",
      "**Backend data access pattern:**  ",
      "Dapper wrapping existing SPs in the repository layer. Multi-result-set SPs consumed via `IProcInfoReader.ReadAsync` — fetch result sets by table name, never by ordinal. Object mapping via Mapster (`source.Adapt<Destination>()`); no manual property-assignment chains.",
      "",
      "**API contract (high level):**  ",
      "${61:Endpoints expected — typically GET (load), GET `/dropdown-options` (backend-owned enums), PUT/PATCH (save) with `rowVersion`. Reference the api-contract.md deliverable. All endpoints return the standardized response envelope.}",
      "",
      "**Expected class shape (follow APCFG pattern):**  ",
      "${62:${1:Menu Code}Controller, I${1:Menu Code}Service / ${1:Menu Code}Service, I${1:Menu Code}Repository / ${1:Menu Code}Repository, ${1:Menu Code}Dto, ${1:Menu Code}Entity, Update${1:Menu Code}Request, ${1:Menu Code}Validator, ${1:Menu Code}MappingConfig}",
      "",
      "**Dropdown / lookup sources:**  ",
      "${63:GL accounts (via GLAccountSelector), cost centers, payment methods, formats, etc. Enums defined backend-side with `[Description]` and exposed via `GET /api/{Module}/dropdown-options`; never duplicated in TypeScript. Use centralized `EnumHelper.GetOptions<T>()` / `BuildEnumConverter<T>()`.}",
      "",
      "**Integrations touched:**  ",
      "${64:GL posting, AP export, remittance file format, RabbitMQ events, third-party systems, or N/A}",
      "",
      "**Audit / compliance:**  ",
      "${65:Mark relevant entity properties with `[Auditable]`; entity implements `IAuditDescribable`; `Description` and `GenealogyDescription` built via `AuditDescriptionBuilder`; `AuditChangeDetector` produces human-readable old→new with field captions matching the UI; use `EnumHelper.BuildEnumConverter<T>()` for enum-valued fields.}",
      "",
      "**Concurrency:**  ",
      "${66:If the entity carries a `ts` / `rowVersion` column, the save endpoint must round-trip it. Frontend wires `useConcurrencyConflict()` and renders `<ConcurrencyConflictDialog>` on 409.}",
      "",
      "**Per-tenant routing:**  ",
      "${67:Standard EnterpriseNext middleware handles per-tenant DB routing; no per-feature work expected unless this screen is multi-tenant in a non-standard way.}",
      "",
      "**Volume / timing / sync considerations:**  ",
      "${68:Configuration screens are low-volume; flag if high-volume or batch-driven.}",
      "",
      "**Test data / fixtures:**  ",
      "${69:Any specific tenant, role, or seed data needed to exercise the screen during integration / E2E tests.}",
      "",
      "**Other data notes:**  ",
      "${70:Anything else specific to this screen.}",
      "",
      "---",
      "",
      "## 9. Success Metrics",
      "",
      "**Parity metrics:**",
      "- ${71:100% of legacy fields/tabs reproduced (control hierarchy validated against `research.md`)}",
      "- ${72:100% of legacy validations reproduced with equivalent behavior}",
      "- ${73:0 customer-reported parity defects within first 30 days of release}",
      "",
      "**System metrics:**",
      "- ${74:API response time within agreed SLO; no regression vs other migrated screens}",
      "- ${75:All Vitest + xUnit + Playwright tests passing; ≥80% line coverage on new code}",
      "- ${76:Change-auditor agent PASS on every `US-*` commit}",
      "",
      "**Business / program metrics:**",
      "- ${77:Menu code marked complete in Bootcamp 2.0 epic tracker (ADO 1365330)}",
      "- ${78:Unblocks customers on the SaaS migration list who depend on ${1:Menu Code}}",
      "",
      "---",
      "",
      "## 10. Code Flow / System Flow Diagram",
      "",
      "Standard EnterpriseNext vertical-slice flow. Adjust nodes only if this screen deviates.",
      "",
      "```mermaid",
      "flowchart TD",
      "  U[User opens ${1:Menu Code} in EnterpriseNext UI] --> R[React feature: frontend/src/features/${79:domain}/${80:feature-folder}]",
      "  R --> RQ[React Query hooks + service module]",
      "  RQ --> API[${60:Target API service e.g. AccountsPayable.Api}]",
      "  API --> CT[${1:Menu Code}Controller]",
      "  CT --> SV[I${1:Menu Code}Service / ${1:Menu Code}Service]",
      "  SV --> VAL[${1:Menu Code}Validator]",
      "  SV --> MAP[Mapster .Adapt&lt;T&gt;]",
      "  SV --> RP[I${1:Menu Code}Repository / ${1:Menu Code}Repository]",
      "  RP --> DAP[Dapper + IProcInfoReader.ReadAsync]",
      "  DAP --> SP[(Legacy SQL Server SPs / tables — READ-ONLY)]",
      "  SV --> AUD[Auditing: AuditChangeDetector on Auditable entity]",
      "  SV --> MQ[RabbitMQ events if applicable]",
      "  CT --> RESP[Standardized response envelope incl rowVersion]",
      "  RESP --> R",
      "  R --> RENDER[Tabs render: ${26:Tab 1} / ${27:Tab 2} / ${28:Tab 3}]",
      "  R --> GUARD[useBlocker + useUnsavedChangesWarning + ConcurrencyConflictDialog on 409]",
      "```",
      "",
      "**Notes:**  ",
      "${81:Call out anything non-standard — e.g., this screen also writes to a queue, calls a third-party API, requires a new dropdown-options endpoint, or shares state with another menu code. Note any new shared components needed (e.g., a new lookup that other migrations could reuse).}",
      "",
      "---",
      "",
      "## 11. Guardrails / Constraints (EnterpriseNext Constitution)",
      "",
      "Full canon: `CLAUDE.md`, `docs/guardrails.md`, `docs/workflow.md`, `.specify/memory/constitution.md`. Each guardrail exists because something already went wrong once.",
      "",
      "**Stack (non-negotiable):**  ",
      "Frontend — React + TypeScript + Vite + MUI 6 (PDI brand theme, light/dark, Hind Siliguri) + React Query + React Hook Form + React Router + i18next + Vitest/RTL + Playwright.  ",
      "Backend — .NET 8+ Web API + Dapper + RabbitMQ + Okta OIDC + xUnit.  ",
      "Infra — Docker / Kubernetes / Azure DevOps CI/CD / SQL Server (existing DBs, zero schema changes).",
      "",
      "**Zero database changes:**  Never modify SQL Server schema or stored procedures.",
      "**Legacy code is READ-ONLY:**  Never create, modify, or delete any file under `Enterprise/` or `EnterpriseWeb/`. These folders are not tracked in the EnterpriseNext repo — modifications are undetectable and irreversible.",
      "**Vertical slice rule:**  No UI without backing API, no API without tests. Spec + plan + data-model + api-contract + prd.json + research + tasks + quickstart + test scaffolding land before implementation code (see `docs/workflow.md §1`).",
      "**Backend-first rule:**  API contract + backend tests precede frontend wire-up.",
      "**Parity rule:**  No silent behavior changes vs legacy. Intentional deltas must be explicit in Section 2 and approved. No scope creep — if it isn't in the PRD, don't build it.",
      "**Internationalization:**  All user-visible strings go through `react-i18next`; keys added to all three locale files (`en`, `es-MX`, `fr-CA`) under `frontend/public/locales/{locale}/${80:feature-folder}.json`.",
      "**Accessibility:**  WCAG 2.1 AA — 4.5:1 contrast, keyboard navigation, ARIA labels, focus order, screen-reader landmarks.",
      "**Dark mode:**  Every component must work in both light and dark MUI themes.",
      "",
      "**Architecture guardrails (10 rules — full detail in `docs/guardrails.md`):**",
      "1. **Backend-owned enums & dropdown options** — `[Description]` on backend enums, exposed via `GET /api/{Module}/dropdown-options`; never duplicate in TS.",
      "2. **Localization** — every user-visible string through `react-i18next`; keys in all three locales.",
      "3. **Change auditing** — `[Auditable]` + `AuditChangeDetector` + `IAuditDescribable` + `Description` / `GenealogyDescription` via `AuditDescriptionBuilder`; field names match on-screen captions (drop redundant entity prefixes); enum-valued fields use `EnumHelper.BuildEnumConverter<T>()`.",
      "4. **Centralized EnumHelper** — `EnumHelper.GetOptions<T>()` / `BuildEnumConverter<T>()`; no per-service helpers.",
      "5. **Mapster for object mapping** — `source.Adapt<Destination>()`; no manual property-assignment chains.",
      "6. **Optimistic concurrency** — every `ts`-bearing entity round-trips `rowVersion` on save; UI wires `useConcurrencyConflict()` + `<ConcurrencyConflictDialog>` on 409.",
      "7. **Multi-result-set SPs** — `IProcInfoReader.ReadAsync`; fetch by table name, never by ordinal.",
      "8. **Tabbed detail/config screens** — one layout: `<Paper>` wrapping `<Tabs>` + active-tab content + `<FormActionBar>`; Save gated on `!isDirty || isSubmitting`.",
      "9. **Form control sizing** — `size: 'small'` set in MUI theme `defaultProps`; no per-component overrides.",
      "10. **Unsaved changes protection** — every edit screen wires both `useBlocker` and `useUnsavedChangesWarning(isDirty)`.",
      "",
      "**Change-Auditor gate (mandatory):**  Any commit referencing a `US-*` story must be reviewed by the `change-auditor` agent and tagged `[audited]` in the commit message. A `PreToolUse` Bash hook blocks unaudited `US-*` commits. Non-story commits (config, docs, refactors) are not gated.",
      "",
      "**Permissions:**  ${82:Legacy role keys mapped to EnterpriseNext Okta/role/claim model; read vs write enforced server-side; UI hides edit affordances when user lacks write.}",
      "**Performance / latency:**  ${83:No regression vs other migrated screens; typical load <1s on a warm backend.}",
      "**Branch convention:**  `${125:branch-prefix}/${1:Menu Code}` (replace `${125:branch-prefix}` per local convention; reference: `bootcamp/<initials>-${1:Menu Code}` was used by the APCFG vertical slice). Working specs may live under `/specs/${125:branch-prefix}/${1:Menu Code}/` during development; merged/canonical specs live under `/specs/${84:NNN}-${85:kebab-feature-name}/`.",
      "**Design system alignment:**  ${86|Full,Partial,Transitional,Intentionally EnterpriseNext-specific,Not applicable|}",
      "**Design library:**  PDI Design Library (Figma) — https://www.figma.com/design/dW48RGbMMT9MtY1HZQsD6r",
      "",
      "---",
      "",
      "## 12. Edge Cases / Failure States",
      "",
      "- ${87:Conditional UI: field/tab visibility depends on another field value — must match legacy logic and be reflected in `research.md` hierarchy}",
      "- ${88:Read-only access when user lacks edit permission (server-enforced, UI hides edit affordances)}",
      "- ${89:Validation error path — server rejects save; UI surfaces field-level errors via React Hook Form}",
      "- ${90:Concurrent edit — 409 response triggers `<ConcurrencyConflictDialog>` via `useConcurrencyConflict()`}",
      "- ${91:Unsaved changes guard — `useBlocker` (in-app) + `useUnsavedChangesWarning` (browser refresh/close) both wired}",
      "- ${92:Dark mode rendering — verify every state in both themes}",
      "- ${93:Edge case unique to this screen or TBD}",
      "",
      "---",
      "",
      "## 13. Decisions Already Made",
      "",
      "- Stack is React + TypeScript + Vite + MUI 6 (frontend) and .NET 8+ Web API + Dapper (backend) in the `EnterpriseNext` repo — not relitigated per screen",
      "- Zero SQL Server schema or SP changes — wrap existing SPs only",
      "- Legacy `Enterprise/` and `EnterpriseWeb/` are READ-ONLY source-of-truth references; no edits, ever",
      "- Spec-kit artifacts (spec.md, plan.md, data-model.md, api-contract.md, prd.json, research.md, tasks.md, quickstart.md, checklists/requirements.md) are required before implementation",
      "- 10 architecture guardrails apply to every menu code; new patterns require an update to `docs/guardrails.md` and possibly `.specify/memory/constitution.md`",
      "- Change-auditor agent gate is mandatory for `US-*` commits",
      "- All UI strings go through i18next in en/es-MX/fr-CA",
      "- All forms use React Hook Form + `@hookform/resolvers`; all server state via React Query",
      "- Dark mode and WCAG 2.1 AA are baseline, not optional",
      "- ${94:Screen-specific decision already made, e.g. \"keep all 4 legacy tabs\" or \"drop deprecated field X\" — or None}",
      "",
      "---",
      "",
      "## 14. Known Unknowns / Discovery Questions",
      "",
      "- ${95:Are there legacy fields that are no longer used and should be dropped vs ported?}",
      "- ${96:Are there validation rules in the legacy SP that aren't documented and need to be reverse-engineered?}",
      "- ${97:Does any other menu code read/write the same data and need to be coordinated?}",
      "- ${98:Are new lookup/dropdown endpoints required, and do they belong in this service or a shared one?}",
      "- ${99:Discovery question specific to this screen}",
      "",
      "---",
      "",
      "## 15. Decision Ownership",
      "",
      "**Product decision owner:** ${100:PO name or TBD}  ",
      "**Functional / parity SME:** ${101:legacy Enterprise SME or TBD}  ",
      "**Engineering decision owner:** ${102:Bootcamp 2.0 engineering lead or TBD}  ",
      "**Architecture / Constitution reviewer:** ${103:name or TBD}  ",
      "**Required reviewers / SMEs:** ${104:names or TBD}",
      "",
      "Decision ownership should be explicit before feature specs are finalized. If ownership is unclear, the PO should not treat open questions as resolved.",
      "",
      "---",
      "",
      "## 16. Risks",
      "",
      "| Risk | Impact | Mitigation / Follow-up |",
      "|---|---|---|",
      "| Hidden legacy behavior not captured in help docs | Parity defect after release | ${105:SME walkthrough + legacy WinForms + SP review during Analyze phase (Step 1 of vertical slice)} |",
      "| Shared dependency on another not-yet-migrated menu code | Blocks delivery or forces partial parity | ${106:Identify in `research.md`; coordinate sequencing in Bootcamp 2.0 plan} |",
      "| New lookup/dropdown endpoint required | Adds backend scope and possibly cross-service work | ${107:Decide in `plan.md` whether the lookup is screen-local or shared; backend owns the enum} |",
      "| Audit field captions drift from on-screen labels over time | Audit log readability degrades | ${108:Wire captions through a shared constant or rely on form labels} |",
      "| ${109:Other risk} | ${110:impact} | ${111:mitigation} |",
      "",
      "---",
      "",
      "## 17. SpecKit / Claude Code Handoff Summary",
      "",
      "**Primary workflow:**  ",
      "${112:Migrate ${1:Menu Code} (${2:Screen Title}) from legacy Enterprise WinForms to EnterpriseNext React + .NET 8+ at full parity, following the Bootcamp 2.0 vertical-slice pattern demonstrated by APCFG (PR 178922).}",
      "",
      "**Current-state constraints:**  ",
      "${113:Legacy WinForms screen is the source of truth for behavior; help URL is the entry point for SMEs; legacy SPs/tables back the data and may not be modified.}",
      "",
      "**Key personas:**",
      "- ${114:Back-office persona from Section 5}",
      "",
      "**Must-pass scenarios:**",
      "- Happy-path load → edit → validate → save → audit, matching legacy outputs and writing `[Auditable]` change records",
      "- Read-only when user lacks edit permission (server-enforced)",
      "- Optimistic concurrency 409 surfaces `<ConcurrencyConflictDialog>`",
      "- i18n in en, es-MX, fr-CA; dark + light themes; WCAG 2.1 AA",
      "- ${115:Screen-specific must-pass scenario}",
      "",
      "**Data / integration constraints:**  ",
      "${116:Target API service from Section 8; Controller / Service / Repository / Validator / Dto / Entity / MappingConfig classes follow APCFG pattern; Dapper + `IProcInfoReader.ReadAsync` for SP access; Mapster for object mapping; auditing via `[Auditable]` + `AuditChangeDetector`; RabbitMQ if events are emitted.}",
      "",
      "**Code-flow context:**  ",
      "See Section 10 — standard EnterpriseNext vertical-slice flow with this screen's specifics filled in.",
      "",
      "**Hard guardrails (engineering canon):**",
      "- Zero DB / SP changes; legacy code READ-ONLY",
      "- 10 architecture guardrails in `docs/guardrails.md` (enums, i18n, audit, EnumHelper, Mapster, concurrency, ProcInfoReader, tabbed layout, form sizing, unsaved-changes)",
      "- Vertical slice + backend-first + parity + change-auditor agent",
      "- ${117:Screen-specific hard guardrail or N/A}",
      "",
      "**Known edge cases:**",
      "- ${118:Edge case from Section 12}",
      "",
      "**Vertical-slice delivery steps (per `docs/workflow.md`):**",
      "1. **Analyze** — read WinForms source + WCF service + SPs; document control hierarchy in `research.md`; capture screenshots to `docs/images/${1:Menu Code}-*.png`",
      "2. **API** — .NET Web API (Controller + Service + Repository) with xUnit unit + integration tests",
      "3. **UI** — React view consuming the API; tree mirrors documented hierarchy",
      "4. **Test** — Vitest unit + integration; Playwright E2E for primary workflow; ≥80% line coverage on new code",
      "5. **Validate** — human validation gate against legacy WinForms behavior",
      "6. **Complete** — update burndown; run change-auditor; commit with `[audited]`",
      "",
      "**Expected spec-kit deliverables (PO + engineering, not PM):**",
      "- Working drafts under `/specs/${125:branch-prefix}/${1:Menu Code}/` (or developer branch path)",
      "- Canonical merged spec under `/specs/${84:NNN}-${85:kebab-feature-name}/`:",
      "  - `spec.md`",
      "  - `plan.md`",
      "  - `data-model.md`",
      "  - `contracts/api-contract.md`",
      "  - `prd.json`",
      "  - `research.md` (includes control hierarchy + screenshot references)",
      "  - `tasks.md`",
      "  - `quickstart.md`",
      "  - `checklists/requirements.md`",
      "- PRD source: `PRDs/${85:kebab-feature-name}/` (markdown + `.json`)",
      "- Feature `CONTEXT.md` co-located with the React feature directory: `frontend/src/features/${79:domain}/${80:feature-folder}/CONTEXT.md`",
      "",
      "---",
      "",
      "## 18. References & Sources Used",
      "",
      "A project brief with no references should be treated as incomplete unless it is explicitly marked as a placeholder or research-light draft.",
      "",
      "| Source | Type | What it contributed | Link / Location |",
      "|---|---|---|---|",
      "| ADO Feature ${1:Menu Code} | ADO | Feature work item for this menu code | ${119:link} |",
      "| Parent Epic — Bootcamp 2.0 | ADO Epic | Program context and sequencing | https://pdidev.visualstudio.com/PDI/_workitems/edit/1365330 |",
      "| Reference migration — APCFG | ADO + PR | Shape-of-the-work example (spec.md / plan.md / data-model.md / api-contract.md / prd.json / tests pattern) | ADO 1365334 / PR 178922 |",
      "| Engineering CLAUDE.md | Repo doc | Stack, conventions, vertical-slice steps, change-auditor gate | EnterpriseNext repo root |",
      "| docs/guardrails.md | Repo doc | 10 architecture rules (enums, i18n, audit, concurrency, ProcInfoReader, layout, etc.) | EnterpriseNext repo |",
      "| docs/workflow.md | Repo doc | Story execution order, commit format, testing discipline, legacy-source analysis | EnterpriseNext repo |",
      "| .specify/memory/constitution.md | Repo doc | Constitution principles (governs any doc conflicts) | EnterpriseNext repo |",
      "| Legacy Enterprise help page | Help docs | Authoritative description of legacy screen behavior | ${7:https://help.pditechnologies.com/enterprise/#/home/XXXX/X/X} |",
      "| Confluence — Enterprise Retail Bootcamp 2.0 | Confluence | Program scope, principles, and sequencing | https://pdisoftware.atlassian.net/wiki/spaces/EPD/pages/13047332997 |",
      "| EnterpriseNext repo | Code | Target codebase | https://pdidev.visualstudio.com/PDI/_git/EnterpriseNext |",
      "| PDI Design Library (Figma) | Design | Component library / tokens / themes | https://www.figma.com/design/dW48RGbMMT9MtY1HZQsD6r |",
      "| SME interview / walkthrough | Interview | Captured undocumented legacy behavior | ${120:notes link or TBD} |",
      "| ${121:Other source} | ${122:type} | ${123:contribution} | ${124:link} |"
    ],
    "description": "PDI Project Brief template for EnterpriseNext menu-code migrations (Bootcamp 2.0) — aligned with the engineering team's CLAUDE.md, docs/guardrails.md, docs/workflow.md, and .specify/memory/constitution.md. Optimized for SpecKit + Claude Code handoff."
  }
}
